language: ruby
rvm:
  - 2.6.3
  branches:
  only:
  - main
install: gem install jekyll && gem install jekyll-sitemap && gem install jekyll-inline-svg && gem install s3_website
script: jekyll build
after_success: s3_website push